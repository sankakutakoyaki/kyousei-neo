"use strict"

// -------------------------------------------------------------------------------------------------------------------------------------- 初期化時処理

window.addEventListener("load", async () => {
    hamburgerItemAddSelectClass('.header-title', 'list')
});